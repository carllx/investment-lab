# Agent Guidelines

This repository provides tools and analytics for mutual fund / portfolio selection and performance analysis.

## Agent skills

### Issue tracker

GitHub Issues is used for tracking tasks, bugs, and specifications. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical 5-role triage label vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (`CONTEXT.md` at root and `docs/adr/` for Architecture Decision Records). See `docs/agents/domain.md`.
